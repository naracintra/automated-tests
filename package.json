{
  "devDependencies": {
    "cypress": "^12.17.0"
  },
  "name": "newsela-tests",
  "version": "1.0.0",
  "description": "newsela tests",
  "main": "index.js",
  "scripts": {
    "cypress": "cypress open",
    "report": "cypress run --headless --browser chrome --config video=false --reporter mochawesome",
    "report:headed": "cypress run --headed --browser chrome --config video=false --reporter mochawesome",
    "spec": "cypress run --headless --browser chrome --config video=false --reporter mochawesome --spec",
    "spec:headed": "cypress run --headed --config video=false --reporter mochawesome --spec",
    "test": "cypress run --headed --browser chrome"
  },
  "keywords": [],
  "author": "Nara Barros",
  "license": "ISC",
  "dependencies": {
    "chance": "^1.1.5",
    "cypress": "",
    "cypress-file-upload": "^4.1.1",
    "cypress-xpath": "^1.6.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.2.0"
  }
}